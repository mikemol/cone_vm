#!/usr/bin/env bash
set -euo pipefail

if command -v mise >/dev/null 2>&1; then
  MISE_DISABLE_TOOLS="${MISE_DISABLE_TOOLS:-ghc,cabal}" \
    mise exec -- python scripts/policy_check.py --workflows
else
  python scripts/policy_check.py --workflows
fi
